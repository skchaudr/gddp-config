schema_version: '1.0'
schema_type: node
node_id: node-01-gate-smoke
title: Gate smoke test — create a report file
type: capability
why: Verify the gate-token mechanism fires on verdict pass through the real heartbeat
  loop.
depends_on: []
acceptance_criteria:
- id: report-exists
  criterion: reports/gate-smoke.md exists in the repo root and contains the word present
constraints:
- Create only reports/gate-smoke.md in the repo root
- Commit all work on this node's result ref per the executor contract
- Read-only outside reports/
allowed_execution_modes:
- droid
required_artifacts:
- reports/gate-smoke.md
status: deferred
priority: high
unlocks: []
