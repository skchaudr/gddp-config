schema_version: '1.0'
schema_type: node
node_id: node-02-decision-schema
title: Define the canonical decision-object schema
type: capability
why: Stage A. One authoritative shape for a decision object - every later stage consumes it.
depends_on:
- node-01-icm-scaffold
acceptance_criteria:
- id: schema-written
  criterion: '01-decisions/output/decision-schema.md defines the canonical decision object: field names, types, required vs
    optional, and two filled examples drawn from real MyAPI decisions'
- id: fields-cover-memo
  criterion: 'The schema can express the fixed evaluation set: what was decided, rationale, supersession (replaces/replaced-by),
    and relationships between decisions'
constraints:
- 'Thin-node contract: this yaml schedules the work; the semantic room''s CONTEXT.md and tasks/<task>.md hold the working
  instructions. Read them first.'
- Follow 01-decisions/tasks/decision-schema.md; write only under 01-decisions/
- 'Execution trio (operator direction 2026-08-13): main pi session runs gpt-5.6-sol; delegate with up to 5 subagents on model
  xai/grok-4.6 via the subagent tool''s model parameter; close with one oracle or reviewer pass before returning'
allowed_execution_modes:
- pi_rpc
required_artifacts:
- 01-decisions/output/decision-schema.md
status: pending
priority: high
unlocks: []
