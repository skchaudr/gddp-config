schema_version: '1.0'
schema_type: node
node_id: node-06-build-corpus
title: Build the fresh reproducible corpus snapshot
type: capability
why: Stage D. A corpus generated from validated decisions by a repeatable process - not hand-assembled.
depends_on:
- node-05-validate-decision-set
acceptance_criteria:
- id: corpus-exists
  criterion: 02-corpus/output/corpus/ contains the generated corpus (one markdown document per decision plus an index), built
    by 02-corpus/build_corpus.py from decisions-canonical.jsonl
- id: builder-script
  criterion: 02-corpus/build_corpus.py runs with stdlib only and regenerates the corpus from the canonical jsonl
- id: rebuild-identical
  criterion: '02-corpus/output/repro-report.md shows a clean rebuild of the corpus into a temp dir diffed against 02-corpus/output/corpus/:
    identical file set and bytes, or every difference explained and fixed'
constraints:
- The governing memo is /Users/sab-mini/repos/GDDP v MyAPI Part 1 - the first slice.md - read it first.
- Multi-home project values (e.g. MyAPI/Pi/GDDP per the schema slash-join) MUST get an explicit frontmatter/grouping mapping
  defined in this node - record the rule in 02-corpus/CONTEXT.md so later stages do not re-litigate taxonomy
- 'Execution trio (operator direction 2026-08-13): main pi session runs gpt-5.6-sol; delegate with up to 5 subagents on model
  xai/grok-4.6 via the subagent tool''s model parameter; close with one oracle or reviewer pass before returning'
allowed_execution_modes:
- pi_rpc
required_artifacts:
- 02-corpus/build_corpus.py
- 02-corpus/output/repro-report.md
status: pending
priority: high
unlocks: []
