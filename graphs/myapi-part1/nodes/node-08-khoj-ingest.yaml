schema_version: '1.0'
schema_type: node
node_id: node-08-khoj-ingest
title: Ingest the snapshot into a clean Khoj corpus
type: capability
why: Stage F. Load the transformed corpus into a clean target corpus on the discovered Khoj deployment.
depends_on:
- node-07-khoj-transform
acceptance_criteria:
- id: ingested
  criterion: 03-ingestion/output/ingest-report.md records the exact ingestion method, the target corpus name, and Khoj's own
    confirmation (API response, logs, or UI state) that the files are present
- id: clean-target
  criterion: The target corpus was empty or newly created for this run - stated in the report
constraints:
- The governing memo is /Users/sab-mini/repos/GDDP v MyAPI Part 1 - the first slice.md - read it first.
- If credentials are needed and unavailable, record precisely what blocked and stop - never guess secrets
- 'Isolated empty Khoj user for this run: email gddp-part1@local on khoj-38-b. Token lives only on the VM at /home/sab-mini/.khoj/gddp-part1.token. Auth as Authorization: Bearer <token>. Verified empty: GET /api/content/files?client=api&truncated=true&page=0 returns {"files":[],"num_pages":0}. Do not write to default@example.com (3941 files). Read the token via ssh khoj-38-b; do not commit it or print it into the report.'
- 'Execution trio (operator direction 2026-08-13): main pi session runs gpt-5.6-sol; delegate with up to 5 subagents on model
  xai/grok-4.6 via the subagent tool''s model parameter; close with one oracle or reviewer pass before returning'
allowed_execution_modes:
- pi_rpc
required_artifacts:
- 03-ingestion/output/ingest-report.md
status: complete
priority: high
unlocks: []
