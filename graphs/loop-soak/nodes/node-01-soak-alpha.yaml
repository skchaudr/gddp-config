schema_version: '1.0'
schema_type: node
node_id: node-01-soak-alpha
title: Write soak/alpha.txt marker
type: capability
why: Loop-soak filler node — content is meaningless by design; the loop run is
  the product under test.
depends_on: []
acceptance_criteria:
- id: marker-exact
  criterion: soak/alpha.txt exists and its entire content is exactly SOAK-ALPHA-OK
    (one line, no extra text)
- id: report-exists
  criterion: reports/loop-soak/node-01.md exists and states the exact command used
    to verify the file content
constraints:
- Create exactly one new file under soak/ plus the report; change nothing else
- Work only in the node worktree; commit on the node's result ref
allowed_execution_modes:
- pi_rpc
required_artifacts:
- reports/loop-soak/node-01.md
status: ready
priority: high
unlocks: []
