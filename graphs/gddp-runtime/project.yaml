schema_version: "1.0"
schema_type: project

project_id: gddp-runtime
title: GDAD Runtime Engine
repo: skchaudr/gddp-runtime

# Why this project exists in the graph:
# gddp-runtime is the control plane itself. Nodes here represent
# infrastructure capabilities the system needs to become self-operating.
# The forward loop (dispatch) already works. This project closes the
# return loop so merged PRs automatically advance graph state.

nodes:
  - id: return-router
    title: Implement return router — merged PR closes the loop
    status: complete
    type: infrastructure

  - id: openclaw-spec
    title: Write OpenClaw v0 spec — purpose, inputs, outputs, decision loop, failure modes
    status: complete
    type: infrastructure

  - id: openclaw-v0-runtime
    title: Build OpenClaw v0 runtime — event-driven reasoning module in gddp-runtime
    status: pending
    type: infrastructure

  - id: openclaw-v0-review-gate
    title: Build OpenClaw v0 review gate — lightweight PR eval before accepting node
    status: pending
    type: infrastructure
