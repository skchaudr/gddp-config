schema_version: "1.0"
schema_type: node

node_id: gateway-operating-baseline
title: Gateway operating baseline
type: capability

why: Establish stable gateway config, auth, Tailscale, hooks, and startup behavior

depends_on: 
  - host-role-alignment

acceptance:
  - gateway bind/auth/tailscale settings are documented and match intended role on each active gateway host

constraints: []

allowed_execution_modes:
  - jules

required_artifacts:
  - decision.md
  - result-summary.md
  - graph-update.yaml

status: pending
priority: high

unlocks:
  - runtime-health-surface
