schema_version: "1.0"
schema_type: node

node_id: gate-continuation-smoke-b
title: Consume the first result and produce the second gate marker
type: capability

why: Prove that downstream work starts from the passing predecessor's result.

depends_on:
  - gate-continuation-smoke-a

acceptance_criteria:
  - id: marker-a-inherited
    criterion: gate-smoke/a.txt exists unchanged and contains exactly "A passed\n"
  - id: marker-b-exact
    criterion: gate-smoke/b.txt exists and contains exactly "B passed\n"

constraints:
  - Preserve gate-smoke/a.txt unchanged
  - Do not modify graph configuration or runtime databases
  - Add only gate-smoke/b.txt

allowed_execution_modes:
  - agent

required_artifacts: []

status: pending
priority: high

unlocks: []
