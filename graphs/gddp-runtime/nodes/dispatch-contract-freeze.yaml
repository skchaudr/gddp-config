schema_version: "1.0"
schema_type: node

node_id: dispatch-contract-freeze
title: Dispatch contract freeze
type: capability

why: Freeze node/job/result artifact contract so agents stop improvising shape

depends_on: 
  - repo-truth-alignment

acceptance:
  - node/job/result contract is explicitly documented using existing schema/docs structure
  - runtime prompts/adapters reject malformed job metadata that violates the frozen contract

constraints: []

allowed_execution_modes:
  - jules

required_artifacts:
  - decision.md
  - result-summary.md
  - graph-update.yaml
  - patch.diff
  - merged_pr

status: pending
priority: high

unlocks:
  - human-review-gates
  - project-onboarding-lane
