schema_version: "1.0"
schema_type: node

node_id: node-09-build-lint-pass
title: Verify clean build and lint pass on new portfolio
type: capability

why: Guarantee that all TypeScript, MDX, and TanStack Start route tree generation pass without errors or broken links.

depends_on:
  - node-07-tanstack-vercel-preset
  - node-08-meta-seo-resume

acceptance_criteria:
  - id: build-succeeds
    criterion: bun run build (or npm run build) completes with exit code 0
  - id: lint-succeeds
    criterion: npm run lint completes cleanly

constraints:
  - Do not introduce type errors or lint suppressions unnecessarily
  - Ensure routeTree.gen.ts is cleanly generated

allowed_execution_modes:
  - local_subprocess

required_artifacts:
  - src/routeTree.gen.ts

status: pending
priority: high

unlocks:
  - node-10-vercel-domain-checklist
