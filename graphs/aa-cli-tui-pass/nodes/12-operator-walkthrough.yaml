schema_version: "1.0"
schema_type: node
node_id: operator-walkthrough
title: Replace the stale AA_TEST_WALKTHROUGH.md with a Deck-home walkthrough
type: capability
why: |
  AA_TEST_WALKTHROUGH.md still describes the old numeric Root lobby
  (0 generate / 1 deck) and is stale vs hub-deck-home. The walkthrough is
  the operator-facing proof the TUI pass worked end to end.
depends_on:
  - tui-deck-runway-pass
  - tui-create-linear-room
  - tui-verify-rooms-polish
  - tui-picker-grammar
  - frontend-integration
acceptance_criteria:
  - id: walkthrough
    criterion: AA_TEST_WALKTHROUGH.md rewritten around Deck-home → create → fire →
      verify ✓✓, executable as a script and matching live behavior.
  - id: peek-receipt
    criterion: Walkthrough records a live peek/receipt dump proving each step
      (not a fixture).
  - id: stale-killed
    criterion: No reference to the numeric root lobby remains in walkthrough/docs.
constraints:
  - Documentation + verification only; no code changes in this node.
allowed_execution_modes:
  - pi
  - human
required_artifacts:
  - AA_TEST_WALKTHROUGH.md
status: planned
priority: high
unlocks:
  - graph-walk-verification
