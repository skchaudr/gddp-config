schema_version: "1.0"
schema_type: node

node_id: node-05-separation-proposal
title: Synthesize findings into the pi-harness separation spec
type: milestone

why: Convert the audit evidence (nodes 01-04) into the spec for the follow-up execution graph that will implement the pi harness separation into a multi-purpose repo (pi-lite / pi-full / pi-studio). This report is the execution graph's authoring input.

depends_on:
  - node-04-tty-validation-probe

acceptance_criteria:
  - id: report-exists
    criterion: reports/05-separation-proposal.md exists and is non-empty
  - id: findings-cited
    criterion: report contains a findings summary that cites reports 01 through 05 by path
  - id: split-proposed
    criterion: report proposes the multi-purpose repo split for pi-lite, pi-full, and pi-studio, with what moves where
  - id: execution-graph-drafted
    criterion: report drafts the follow-up execution graph as a concrete node list (node titles, dependencies, binary acceptance criteria per node)
  - id: open-questions-marked
    criterion: report explicitly lists open questions reserved for human decision

constraints:
  - Read-only against the environment; the only write is the report itself
  - The drafted execution graph is a proposal for human authoring, not a graph to import

allowed_execution_modes:
  - droid

required_artifacts:
  - reports/05-separation-proposal.md

status: provisional
priority: high

unlocks: []
