acceptance_criteria:
- criterion: reports/pi-harness-execution/extensions-execute.md maps every change
    to the audit proposal it implements
  id: changes-mapped
- criterion: report includes output of pi --version (or equivalent load check) run
    after the changes, exiting 0
  id: pi-loads
- criterion: reports/pi-harness-execution/extensions-execute.md exists and is non-empty
  id: report-exists
allowed_execution_modes:
- factory_mission
constraints:
- Only implement proposals from the paired audit report; no unilateral improvements
- Do not modify settings.json auth or credential content
- Commit all work on this node's result ref per the executor contract
depends_on:
- node-05-extensions-audit
human_gate: true
node_id: node-06-extensions-execute
priority: high
required_artifacts:
- reports/pi-harness-execution/extensions-execute.md
schema_type: node
schema_version: '1.0'
status: pending
title: Implement accepted extensions improvements
type: capability
unlocks: []
why: 'Implement the high-confidence proposals from the paired audit report at reports/pi-harness-execution/extensions-audit.md.
  Each change must trace to a specific proposal in that report. Bounded: skip anything
  rated speculative.'
