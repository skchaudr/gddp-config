schema_version: '1.0'
schema_type: node
node_id: node-05-extensions-audit
title: Audit extensions, hooks, advisor modes for friction and safety
type: capability
why: 'Audit the extensions surface (agent/extensions, hooks, advisor modes) from the
  friction point of view: what causes friction in interactive use, which harness safety
  mechanisms are used or NOT used, language guards, harness hooks, and steering in
  general, brief audit of advisor mode, gaps and improvements.'
depends_on: []
acceptance_criteria:
- id: report-exists
  criterion: reports/pi-harness-execution/extensions-audit.md exists and is non-empty
- id: evidence-cited
  criterion: every finding cites a file:line or command output from the live harness
- id: acceptance-criteria-included
  criterion: report concludes with actionable acceptance criteria for node-06-extensions-execute
constraints:
- 'Read-only: do not modify the harness; the only new file is the report under reports/pi-harness-execution/'
- Verify every claim against the live checkout before writing it
- Commit all work on this node's result ref per the executor contract
allowed_execution_modes:
- factory_mission
required_artifacts:
- reports/pi-harness-execution/extensions-audit.md
status: provisional
priority: high
unlocks: []
