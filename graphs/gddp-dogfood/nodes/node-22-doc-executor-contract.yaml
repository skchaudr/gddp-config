schema_version: '1.0'
schema_type: node
node_id: node-22-doc-executor-contract
title: Document the NodePacket / PatchResult contract
type: capability
why: One-file documentation/cleanup so the post-demolition runtime is legible to the
  next session.
depends_on: []
acceptance_criteria:
- id: artifact-exists
  criterion: docs/executor-contract.md exists and is non-empty
- id: covers
  criterion: 'the file covers the executor_protocol.py field reference: every NodePacket
    and PatchResult field with type and one-line meaning, quoted from code'
- id: suite-unchanged
  criterion: python3 -m pytest -q shows no new failures beyond those already present
    on this host before the node (flask-import and rig1 plist asserts); quote the
    tail line in the commit message
constraints:
- Create or modify only docs/executor-contract.md
- Read-only outside that path
- Commit all work on this node result ref per the executor contract
allowed_execution_modes:
- local_subprocess
required_artifacts:
- docs/executor-contract.md
status: ready
priority: medium
unlocks: []
