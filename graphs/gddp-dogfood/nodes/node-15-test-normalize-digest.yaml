schema_version: '1.0'
schema_type: node
node_id: node-15-test-normalize-digest
title: Unit-test _normalize_digest validation
type: capability
why: Pure-function unit coverage for _normalize_digest in scripts/runtime/heartbeat/completion_discipline.py;
  small, independent, mechanically verifiable.
depends_on: []
acceptance_criteria:
- id: test-file-exists
  criterion: scripts/adapters/test_normalize_digest.py exists (or scripts/runtime/heartbeat/test_normalize_digest.py
    matching the target module location) and contains tests for _normalize_digest
- id: behavior-covered
  criterion: the tests assert that accepts 64-char hex case-insensitively and rejects
    non-hex/wrong-length input
- id: suite-green
  criterion: 'python3 -m pytest -q passes with only the 3 known flask-import failures
    (ModuleNotFoundError: flask); the new test file passes'
constraints:
- Create exactly one new test file for _normalize_digest in scripts/runtime/heartbeat/completion_discipline.py
  next to its module
- Do not modify production code or other test files
- Run python3 -m pytest -q before finishing and quote its tail line in the commit
  or a short note
- Commit all work on this node result ref per the executor contract
allowed_execution_modes:
- local_subprocess
required_artifacts:
- scripts/runtime/heartbeat/test_normalize_digest.py
status: ready
priority: medium
unlocks: []
