schema_version: '1.0'
schema_type: node
node_id: node-08-verification-module-map
title: Map the verification lane modules
type: capability
why: The two-lane evaluator is the trust core; a module map makes its shape reviewable
  at a glance.
depends_on: []
acceptance_criteria:
- id: report-exists
  criterion: reports/gddp-dogfood/08-verification-module-map.md exists and is non-empty
- id: all-modules
  criterion: every module under scripts/runtime/verification/ is listed with a one-line
    purpose
- id: lane-flow
  criterion: report describes the deterministic + semantic lane flow and where verdicts
    combine
constraints:
- Create only reports/gddp-dogfood/08-verification-module-map.md
- Read-only outside that report path
- Run python3 -m pytest -q before finishing and quote its tail line in the report
- Commit all work on this node result ref per the executor contract
allowed_execution_modes:
- local_subprocess
required_artifacts:
- reports/gddp-dogfood/08-verification-module-map.md
status: ready
priority: medium
unlocks: []
