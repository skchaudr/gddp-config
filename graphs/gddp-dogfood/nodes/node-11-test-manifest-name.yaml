schema_version: '1.0'
schema_type: node
node_id: node-11-test-manifest-name
title: Unit-test _manifest_name sanitization and digest shape
type: capability
why: Pure-function unit coverage for _manifest_name in scripts/adapters/mission_evidence.py;
  small, independent, mechanically verifiable.
depends_on: []
acceptance_criteria:
- id: test-file-exists
  criterion: scripts/adapters/test_manifest_name.py exists (or scripts/runtime/heartbeat/test_manifest_name.py
    matching the target module location) and contains tests for _manifest_name
- id: behavior-covered
  criterion: the tests assert that safe filename keeps alphanumerics/dash/underscore,
    appends a 12-hex digest, and is deterministic
- id: suite-green
  criterion: 'python3 -m pytest -q passes with only the 3 known flask-import failures
    (ModuleNotFoundError: flask); the new test file passes'
constraints:
- Create exactly one new test file for _manifest_name in scripts/adapters/mission_evidence.py
  next to its module
- Do not modify production code or other test files
- Run python3 -m pytest -q before finishing and quote its tail line in the commit
  or a short note
- Commit all work on this node result ref per the executor contract
allowed_execution_modes:
- local_subprocess
required_artifacts:
- test_manifest_name.py
status: ready
priority: medium
unlocks: []
