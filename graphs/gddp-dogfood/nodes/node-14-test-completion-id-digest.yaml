schema_version: '1.0'
schema_type: node
node_id: node-14-test-completion-id-digest
title: Unit-test _completion_id and _completion_digest stability
type: capability
why: Pure-function unit coverage for _completion_id in scripts/adapters/mission_evidence.py;
  small, independent, mechanically verifiable.
depends_on: []
acceptance_criteria:
- id: test-file-exists
  criterion: scripts/adapters/test_completion_identity.py exists (or scripts/runtime/heartbeat/test_completion_identity.py
    matching the target module location) and contains tests for _completion_id
- id: behavior-covered
  criterion: the tests assert that completion id format is mission:feature:worker
    and the digest is stable across dict ordering
- id: suite-green
  criterion: 'python3 -m pytest -q passes with only the 3 known flask-import failures
    (ModuleNotFoundError: flask); the new test file passes'
constraints:
- Create exactly one new test file for _completion_id in scripts/adapters/mission_evidence.py
  next to its module
- Do not modify production code or other test files
- Run python3 -m pytest -q before finishing and quote its tail line in the commit
  or a short note
- Commit all work on this node result ref per the executor contract
allowed_execution_modes:
- local_subprocess
required_artifacts:
- scripts/adapters/test_completion_identity.py
status: provisional
priority: medium
unlocks: []
