schema_version: '1.0'
schema_type: node
node_id: node-17-doc-local-executor
title: Document the local one-shot executor contract
type: capability
why: One-file documentation/cleanup so the post-demolition runtime is legible to the
  next session.
depends_on: []
acceptance_criteria:
- id: artifact-exists
  criterion: docs/local-subprocess-executor.md exists and is non-empty
- id: covers
  criterion: the file covers the GDDP_LOCAL_SUBPROCESS_ARGV env contract, local_agent_executor.py
    flow (worktree -> agent -> persist_result -> handoff), and the spool directory
    layout, quoting real code paths
- id: suite-unchanged
  criterion: python3 -m pytest -q shows no new failures beyond those already present
    on this host before the node (flask-import and rig1 plist asserts); quote the
    tail line in the commit message
constraints:
- Create or modify only docs/local-subprocess-executor.md
- Read-only outside that path
- Commit all work on this node result ref per the executor contract
allowed_execution_modes:
- local_subprocess
required_artifacts:
- docs/local-subprocess-executor.md
status: provisional
priority: medium
unlocks: []
