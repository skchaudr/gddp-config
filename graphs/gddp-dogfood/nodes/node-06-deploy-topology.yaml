schema_version: '1.0'
schema_type: node
node_id: node-06-deploy-topology
title: Document the deploy/mini-heartbeat tree
type: capability
why: The deploy tree is the production heartbeat; a one-line-per-file map keeps host
  setup reviewable.
depends_on: []
acceptance_criteria:
- id: report-exists
  criterion: reports/gddp-dogfood/06-deploy-topology.md exists and is non-empty
- id: all-files
  criterion: every file under deploy/mini-heartbeat/ is listed with a one-line purpose
- id: quotes-paths
  criterion: report quotes real paths from the live tree
constraints:
- Create only reports/gddp-dogfood/06-deploy-topology.md
- Read-only outside that report path
- Run python3 -m pytest -q before finishing and quote its tail line in the report
- Commit all work on this node result ref per the executor contract
allowed_execution_modes:
- local_subprocess
required_artifacts:
- reports/gddp-dogfood/06-deploy-topology.md
status: provisional
priority: medium
unlocks: []
