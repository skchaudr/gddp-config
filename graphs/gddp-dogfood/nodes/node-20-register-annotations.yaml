schema_version: '1.0'
schema_type: node
node_id: node-20-register-annotations
title: Annotate the blocking-mechanisms register with demolition outcomes
type: capability
why: One-file documentation/cleanup so the post-demolition runtime is legible to the
  next session.
depends_on: []
acceptance_criteria:
- id: artifact-exists
  criterion: docs/blocking-mechanisms-register.md exists and is non-empty
- id: covers
  criterion: the file covers a header note recording that demolition Stages 1-3 executed
    BM-019, BM-020, BM-030 through BM-036 (commits on main), with no other content
    changed
- id: suite-unchanged
  criterion: python3 -m pytest -q shows no new failures beyond those already present
    on this host before the node (flask-import and rig1 plist asserts); quote the
    tail line in the commit message
constraints:
- Create or modify only docs/blocking-mechanisms-register.md
- Read-only outside that path
- Commit all work on this node result ref per the executor contract
allowed_execution_modes:
- local_subprocess
required_artifacts:
- docs/blocking-mechanisms-register.md
status: provisional
priority: medium
unlocks: []
