# 023 — Executor-Neutral Agent Mode

------------------------------------------------ Agent Section START

Date: 2026-07-21
Worktree: /Users/sab-mini/repos/gddp-config-exec-mode
Branch: fix/executor-neutral-mode

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Node-level `agent` is now the canonical executor-neutral execution mode across schema validation, import, and interactive authoring. The five gddp-runtime draft nodes use that machine-readable value; job/event executor enums remain concrete and unchanged.

### Scope touched (One file per line, +/- for only what was changed)

+ schemas/v1/node.yaml
+ scripts/validate.py
+ scripts/import_node.py
+ scripts/new_node.py
+ scripts/batch_fill.py
+ scripts/test_compliance.py
+ graphs/gddp-runtime/nodes/concurrent-node-flow.yaml
+ graphs/gddp-runtime/nodes/direct-executor-round-trip.yaml
+ graphs/gddp-runtime/nodes/graph-frontier-operations.yaml
+ graphs/gddp-runtime/nodes/immediate-evaluator-round-trip.yaml
+ graphs/gddp-runtime/nodes/neutral-executor-contract.yaml

### Constrained areas touched (none / list + justification)

none — node eligibility changed; runtime job/event executor contracts did not.

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Fix commit `a6921c7` is pushed on `origin/fix/executor-neutral-mode`; this handoff is the only subsequent task change. The isolated worktree has no inherited artifacts.

### Artifacts (Filepath - Description, 1 line max per artifact)

scripts/test_compliance.py - Proves `agent` is accepted by every node authoring/validation path and `any agent` is rejected.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Fast-forward this branch to main. Global validation is green with 0 errors; one pre-existing warning remains because return-router unlocks the not-yet-authored `first-overnight-run` node.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
