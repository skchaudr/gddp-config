# 030 — Jules API + local dual-run routing

------------------------------------------------ Agent Section START

Date: 2026-07-26
Worktree: /Users/sab-mini/repos/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Sab directed Node 4 to use one Jules API executor and one local executor.
Project default is now `jules_api`; `heartbeat-crash-recovery` selects it and
`pi-evaluator-harness` selects `local_subprocess`. No graph status, dependency,
acceptance criterion, or required artifact changed.

### Scope touched (One file per line, +/- for only what was changed)

- `graphs/gddp-runtime/project.yaml` (default executor + update date)
- `graphs/gddp-runtime/nodes/heartbeat-crash-recovery.yaml` (executor mode)
- `graphs/gddp-runtime/nodes/pi-evaluator-harness.yaml` (executor modes)
- `schemas/v1/{node,job,event}.yaml` (current runtime executor names)
- `scripts/{validate,import_node,new_node,batch_fill}.py` (matching enum)
- `scripts/test_compliance.py` (runtime executor enum coverage)

### Constrained areas touched (none / list + justification)

- Human-owned graph definitions: executor routing only, implementing Sab's
  explicit dual-run/default-Jules direction; graph truth/status unchanged.

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Routing/tooling commit: `d34535a`. Validation has zero errors; the existing
`return-router` dangling-unlock warning remains.

### Artifacts (Filepath - Description, 1 line max per artifact)

- `.handoffs/030-jules-api-dual-run-routing.md` - exact routing and resume point.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Inject one tagged event for each routed node, then run one controlled heartbeat
to reserve and dispatch both. Stop at `awaiting_review`; only Sab may change
node status.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
