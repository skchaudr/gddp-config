# 056 — Stop verification/ timestamp and partial-export churn

------------------------------------------------ Agent Section START

Date: 2026-09-10
Worktree: /home/sab-mini/gddp-config
Branch: fix/verification-churn

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

`verification/vault-doctor/auth-node.json` was gitignored as a mock receipt but still tracked, so every test/bridge run rewrote `generated_at`. `verification/pi-harness-execution/evaluations.yaml` was replaced from this host's partial `queue.db`, which dropped node-02, node-06, and node-11. Restored the YAML, untracked the mock, and the exporter in gddp-runtime now merges and skips no-op writes.

### Scope touched (One file per line, +/- for only what was changed)

- .gitignore (comment: why auth-node stays untracked)
- verification/vault-doctor/auth-node.json (git rm --cached; file remains on disk, ignored)
- verification/pi-harness-execution/evaluations.yaml (restored to HEAD; eight nodes kept)
+ .handoffs/056-verification-churn.md

### Constrained areas touched (none / list + justification)

verification receipts — restored dropped nodes rather than committing the partial export. Companion fix is gddp-runtime branch `fix/evaluations-yaml-merge`.

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Branch `fix/verification-churn` off `main`. Mock auth-node untracked; evaluations.yaml matches committed eight-node export.

### Artifacts (Filepath - Description, 1 line max per artifact)

none

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Merge this branch to main and merge `fix/evaluations-yaml-merge` in gddp-runtime so heartbeat export can no longer drop file-only nodes. Copy `scripts/export_evaluations.py` into the live runtime checkout (currently 14 behind origin with unrelated heartbeat dirt) if the timer is still armed on this host.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
