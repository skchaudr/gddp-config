# 050 — TUI usability: running rows, aligned columns, live `w`

------------------------------------------------ Agent Section START

Date: 2026-08-14
Worktree: /Users/sab-mini/repos/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Front-page node rows used variable-width `phase · title` text, so GRAPH/RUNTIME/EVAL/TITLE drifted. `w` was advertised and routed, but `interactive_watch` detoured into fzf `cmd_runs` and returned immediately on empty/cancel. Node picker now uses fixed columns plus a `▶` + reverse running mark; `w` calls `cmd_watch` and keeps missing-runtime failures on screen.

### Scope touched (One file per line, +/- for only what was changed)

+ scripts/gddp.py — aligned node columns, running mark, key-spec sync, watch boundary
+ scripts/test_gddp.py — column, key-sync, and `w`/`cmd_watch` regressions
+ scripts/README.md — paged-menu column contract
+ .handoffs/050-tui-usability-repair.md — this handoff

### Constrained areas touched (none / list + justification)

none — inherited dirty graph YAML and verification receipts were left exactly as found

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Task files committed and pushed on `main`. Inherited `graphs/myapi-part1/*` and `verification/*` dirt remains unstaged on purpose.

### Artifacts (Filepath - Description, 1 line max per artifact)

/tmp/gddp-config-tui-grok46.md — run evidence for the parent acceptance packet

### Resume point (2-3 sentences max, anything more must be critically justifiable)

No implementation follow-up. Press `w` on the front page or graph hub for `gddp watch`; running nodes show `▶running` in the RUNTIME column.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
