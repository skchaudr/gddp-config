# Handoff 015 — Obsidian vault export

## Agent Section

**Date:** 2026-07-02  
**Branch:** main  
**Commit:** (see `git log -1`)

### What shipped
- `scripts/obsidian_export.py` — one-way YAML → Obsidian markdown
- `gddp obsidian export [--project X] [--dry-run]`
- Self-contained vault at `obsidian-vault/` (open folder in Obsidian)
- Generated notes: `obsidian-vault/GDDP/graphs/<project>/<node>.md` (gitignored)
- Starter Graph View color groups in `obsidian-vault/.obsidian/graph.json`

### Design rules enforced
- YAML in `graphs/` is source of truth
- Only `verified` / `owned` frontmatter preserved on re-export
- `depends_on` / `unlocks` → wikilinks; acceptance in body; evidence paths to YAML + verification receipts

### Resume
```bash
.venv/bin/python scripts/gddp.py obsidian export
# Open obsidian-vault/ in Obsidian; filter: path:GDDP/graphs/aa-cli
```

### Next (b) — custom visibility UI
Use this vault as requirements-gathering: which filters, colors, and sign-off flows feel right before building a Lovable/custom solution.

### Git state at handoff
Run `git status --short --branch` — should be clean on main after push.

---

Do NOT edit this file past this point