# 031 — Operator dispatch surface (positional + menu)

------------------------------------------------ Agent Section START

Date: 2026-07-26
Worktree: /Users/sab-mini/repos/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

`gddp <graph|node> [executor]` dispatches through the existing event →
heartbeat pipeline: graph-first resolution, exact node ID otherwise, hard-error
validation (unknown/ambiguous/not-ready/executor-not-allowed), one preview, one
confirm, one schema-valid `manual_inject` event per node. Executor preselection
travels in `events.routing.selected_executor` and is honored by the runtime
classifier (gddp-runtime `a95d23f`) only when the node allows it.

### Scope touched (One file per line, +/- for only what was changed)

- `scripts/gddp.py` (+ positional dispatch, menu entry `d`, preview/confirm, event insert)
- `scripts/test_gddp_dispatch.py` (+ 15 tests)
- gddp-runtime `scripts/runtime/heartbeat/classifier.py` (+ routing consumption)
- gddp-runtime `scripts/runtime/heartbeat/test_classifier_routing.py` (+ 4 tests)

### Constrained areas touched (none / list + justification)

None. No graph YAML/status writes, no launchd, no live dispatch — smoke tests
against the live DB were verified to insert zero events (EOF abort path).

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

gddp-config `063d267` pushed; gddp-runtime `a95d23f` pushed. Both trees clean.
`.venv` gained pytest (local dev tooling, untracked).

### Artifacts (Filepath - Description, 1 line max per artifact)

- `scripts/test_gddp_dispatch.py` — 15 tests: resolution, validation, insert shape, confirm/abort/EOF gates (89/89 config suite green; 75/75 runtime heartbeat green).

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Dispatch is operator-ready: `gddp gddp-runtime <executor>` for the frontier or
`gddp <node>` for one. Remaining before the N4 dual run: Sab's pending reviews
(`pi-evaluator-harness` accept unblocks `pi-evaluator-guard`), then
`gddp verdict-confidence-split` + `gddp pi-evaluator-guard` (both local) as the
acceptance-unblocks-downstream + simultaneous-evaluator proof.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
