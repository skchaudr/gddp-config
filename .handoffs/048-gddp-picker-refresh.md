# 048 — GDDP picker refresh

------------------------------------------------ Agent Section START

Date: 2026-08-14
Worktree: /Users/sab-mini/repos/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

The interactive graph, project, and node pickers now show `r refresh` and re-query graph files plus runtime evidence in place. Regression tests cover the chrome, refresh signal, graph activity reload, and node evidence reload.

### Scope touched (One file per line, +/- for only what was changed)

+ scripts/gddp.py — refreshable paged picker and live graph/node reload loops
+ scripts/test_gddp.py — refresh behavior regressions
+ .handoffs/048-gddp-picker-refresh.md — session handoff

### Constrained areas touched (none / list + justification)

none

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Task files are committed and pushed on main. Runtime-written graph status and evaluation receipt changes remain preserved for the janitor flow.

### Artifacts (Filepath - Description, 1 line max per artifact)

none

### Resume point (2-3 sentences max, anything more must be critically justifiable)

No implementation follow-up remains. Press `r` in a graph, project, or node picker to load current graph/runtime state without leaving the screen.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point
