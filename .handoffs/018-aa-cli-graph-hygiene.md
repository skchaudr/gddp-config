# 018 - aa-cli graph hygiene

------------------------------------------------ Agent Section START

Date: 2026-07-03
Worktree: /home/sab/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Compared `graphs/gddp-runtime` and `graphs/aa-cli` against clean sibling checkouts at `/home/sab/gddp-runtime` commit `5d91cf5` and `/home/sab/aa-cli` commit `edd9a95`, both tracking `origin/main`. Runtime graph truth stayed unchanged: `verification-receipt-contract` remains `ready`, and review-gate/confidence-split criteria are not satisfied enough to complete.

### Scope touched (One file per line, +/- for only what was changed)

+ .handoffs/018-aa-cli-graph-hygiene.md
+ graphs/aa-cli/project.yaml
+ graphs/aa-cli/nodes/common-core.yaml
+ graphs/aa-cli/nodes/target-registry.yaml
+ graphs/aa-cli/nodes/ledger-system.yaml
+ graphs/aa-cli/nodes/dispatch-router.yaml
+ graphs/aa-cli/nodes/dispatch-grok.yaml
+ graphs/aa-cli/nodes/dispatch-pi-cli.yaml
+ graphs/aa-cli/nodes/dispatch-gemini.yaml
+ graphs/aa-cli/nodes/dispatch-droid.yaml
+ graphs/aa-cli/nodes/dispatch-codex.yaml
+ graphs/aa-cli/nodes/dispatch-jules.yaml
+ graphs/aa-cli/nodes/dispatch-pi-harness.yaml
+ graphs/aa-cli/nodes/reconciliation.yaml

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Config started clean on `main` tracking `origin/main`; validation passed with one pre-existing dangling-unlock warning in `graphs/gddp-runtime/nodes/return-router.yaml`. `git fetch --prune` failed because GitHub SSH auth is unavailable in this environment; push may need to be retried from an authenticated shell.

### Artifacts (Filepath - Description, 1 line max per artifact)

graphs/aa-cli/project.yaml - aa-cli node statuses reconciled to complete against pushed aa-cli capabilities.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Resume by checking whether the graph hygiene commit reached `origin/main`; if not, push it from an authenticated shell. No visualization files were touched.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point
