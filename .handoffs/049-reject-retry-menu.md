# 049 — Reject + retry menu

------------------------------------------------ Agent Section START

Date: 2026-08-14
Worktree: /Users/sab-mini/repos/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

The node-review menu now exposes `x reject + retry`: it returns graph truth to ready, records the operator fix-list, and invokes the runtime retry boundary. The launcher sources the canonical mini-heartbeat executor environment so operator dispatch actions have the same configured transport as heartbeat ticks.

### Scope touched (One file per line, +/- for only what was changed)

+ scripts/gddp.py — reject + retry menu and runtime command forwarding
+ scripts/test_gddp.py — menu and forwarding regressions
+ bin/gddp — canonical runtime executor environment loading
+ .handoffs/049-reject-retry-menu.md — session handoff

### Constrained areas touched (none / list + justification)

none

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Task changes are committed and pushed on main. Runtime-written verification export changes remain preserved for the janitor flow.

### Artifacts (Filepath - Description, 1 line max per artifact)

none

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Use `x reject + retry` from a node review or `gddp jobs retry <job> --reason '...'`. Node-08 attempt 2 is already running with the new clean-user fix-list.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point
