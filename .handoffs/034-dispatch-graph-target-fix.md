# 034 — Fix graph-wide dispatch target resolution

------------------------------------------------ Agent Section START

Date: 2026-07-29
Worktree: /Users/sab-mini/repos/gddp-config
Branch: fix/gddp-tui-navigation

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

The interactive blank-node path rejected valid graph IDs because a matching `project_hint` forced node lookup. The dispatch screen also mislabeled all graph-status-ready nodes as the frontier before dependency/runtime checks; live `gddp-runtime` has six graph-ready nodes but only five dispatchable nodes because `pi-evaluator-guard` is dependency-blocked.

### Scope touched (One file per line, +/- for only what was changed)

+ scripts/gddp.py — preserve graph targets, share truthful blocker classification, add one-key graph/target pickers
+ scripts/test_gddp_dispatch.py — regression coverage for graph-wide resolution and truthful picker contents
+ scripts/README.md — document dispatch picker and frontier truth

### Constrained areas touched (none / list + justification)

none

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

The previous navigation commit is clean and tracked by origin on `fix/gddp-tui-navigation`. The three scoped fix files and this handoff are intentionally uncommitted pending commit and final status verification.

### Artifacts (Filepath - Description, 1 line max per artifact)

scripts/gddp.py - Graph/node dispatch target resolver.
scripts/test_gddp_dispatch.py - Graph-wide dispatch regression coverage.
scripts/README.md - Operator-facing dispatch behavior.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Commit the source/test/docs files plus this handoff with the Codex co-author marker. Verify the clean branch, then push; the live frontier probe and all relevant validation already pass.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
