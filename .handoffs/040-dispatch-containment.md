# 040 — Dispatch contract and graph containment

------------------------------------------------ Agent Section START

Date: 2026-08-09
Worktree: `/Users/sab-mini/repos/gddp-config`
Branch: `main`

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Dispatch now persists the concrete executor chosen by its plan and treats `agent` as executor-neutral. Graph-wide dispatch excludes status-drifted or executor-incompatible nodes instead of aborting valid siblings; the provisional gate-token loader also follows the runtime module rename.

### Scope touched (One file per line, +/- for only what was changed)

+ `scripts/gddp.py`
+ `scripts/test_gddp_dispatch.py`
+ `scripts/node_cli.py`
+ `scripts/test_node_cli.py`
+ `.handoffs/040-dispatch-containment.md`

### Constrained areas touched (none / list + justification)

Cross-repo runtime interfaces: dispatch event routing, `gate_tokens.py`, and `node_status_history.py`; no graph definitions or human-owned status changed.

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Task commits are `6022049`, `0467c46`, and `1854a39` on `main`. Full suite passes 179 tests; installed dispatch replay excluded one drifted node and independently reported five in-flight nodes without inserting events.

### Artifacts (Filepath - Description, 1 line max per artifact)

`scripts/test_gddp_dispatch.py` - Composed dispatch-boundary and node-local containment regression tests.
`scripts/test_node_cli.py` - Cross-repo provisional gate revocation integration test.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

BM-007 remains partial only for route-less neutral intake outside planned CLI/frontier events. Next high-value correction is capacity release and head-of-line scanning, not graph/status mutation.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
