# 042 — Arrow-key CSI decode (Ghostty/tmux)

------------------------------------------------ Agent Section START

Date: 2026-08-13
Worktree: /Users/sab-mini/repos/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

khoj-38 `~/gddp-config` TUI code matched local (`terminal.py` same md5; `gddp.py` differed only by watch/steer). Local Ghostty/tmux sends modified CSI (`ESC[1;3A`) and focus (`ESC[I`); the old 3-byte decoder treated those as Escape (refresh/back) and leaked leftovers as spam. Decoder now consumes a full CSI/SS3 and maps arrows; unknown/incomplete sequences return empty and menus ignore them.

### Scope touched (One file per line, +/- for only what was changed)

+ scripts/terminal.py — full CSI/SS3 consume; modified arrows; unknown ≠ Escape
+ scripts/gddp.py — empty getch continues in menu/paged/review loops
+ scripts/test_terminal.py — modified CSI, focus, SS3, incomplete, PTY
+ scripts/test_gddp.py — empty key does not back or spam
+ scripts/README.md — decoder note
+ .handoffs/042-arrow-key-csi-decode.md — this handoff

### Constrained areas touched (none / list + justification)

none

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Working tree has the six files above uncommitted on `main` tracking `origin/main` at `9c3166e` before this commit.

### Artifacts (Filepath - Description, 1 line max per artifact)

scripts/terminal.py - Robust CSI arrow decoder.
scripts/test_terminal.py - Regression coverage for Ghostty/tmux sequences.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

`.venv/bin/python -m unittest scripts.test_terminal scripts.test_gddp -q` OK (63). `scripts/validate.py` 0 errors. After commit+push, pull on khoj-38 is optional (same TUI, this is a local Ghostty/tmux harden).

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
