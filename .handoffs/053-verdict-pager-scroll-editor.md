# 053 — Verdict Pager (Scroll + Open-in-Editor)

Instructions:
- Fill out the date and worktree.
- Follow each heading exactly as instructed; violating this weakens the integrity of the handoff.
- Less is more; the goal is efficiency, not omission.

------------------------------------------------ Agent Section START

Date: 2026-08-19
Worktree: ~/repos/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Evaluator verdict views (node review `e`, more c/d/t, jobs show/results, evaluations drill-in) were one-screenful renders where every key but `u` kicked the operator out — FAIL verdicts run ~174 lines and were unreadable past screen 1. All those views now go through `_page_view`: in-place scrolling (arrows/PgUp/PgDn/j/k/g/G/space), `o` opens the same text in the operator's editor, `u` still passes through to the update flow. terminal.py gained a held-cbreak mode (`cbreak()` ctx + `read_key`) because per-key getch restored canonical mode and stranded typeahead typed during redraws.

### Scope touched (One file per line, +/- for only what was changed)

- scripts/gddp.py — _page_view/_scroll_pause/_wrap_ansi_line/_editor_command/_open_in_editor; wired into 5 view sites; run_runtime_jobs(capture=True)
- scripts/terminal.py — cbreak context manager, read_key, getch refactored onto read_key (behavior identical)
- scripts/test_gddp.py — PagerWrapTests, PagerScrollTests, PagerPageViewTests, EditorCommandTests
- scripts/test_terminal.py — CbreakTypeaheadTests; settle-sleep fix for two pre-existing flaky pty getch tests

### Constrained areas touched (none / list + justification)

none — graph truth, node YAML, runtime job state all untouched; menu `u`-update contracts preserved (tests confirm).

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

main = 88a12a6, pushed (also carried pre-existing ahead commit c80d44c docs). 12 inherited verification/*/evaluations.yaml timestamp bumps were committed separately (chore) after confirming generated_at-only diffs — tree clean and synced as of 6d09fc7. 282 tests pass, validate.py 0 errors.

### Artifacts (Filepath - Description, 1 line max per artifact)

none beyond the commit.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Done and shipped. If follow-up wanted: `gddp watch` drill-in and `interactive_eval_hub` still use plain `_pause`; same `_page_view` treatment would apply if those surfaces get long. Editor override: `GDDP_EDITOR` env beats `VISUAL`/`EDITOR`/nvim default.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)
