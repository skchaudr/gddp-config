# 053 — Detached acceptance worktree; publish refs/heads/<target>

------------------------------------------------ Agent Section START

Date: 2026-08-15
Worktree: /Users/sab-mini/repos/gddp-config-result-surface
Branch: feat/graph-result-surface

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Acceptance worktree stays detached at the target SHA. After merge it publishes HEAD to `refs/heads/<target_branch>` with non-force `git push . HEAD:refs/heads/<branch>`, then a compare-and-swap `update-ref` lease if that branch is already checked out. Ordinary checkout stays on its current branch in both the unrelated-branch and currently-on-target fixtures.

### Scope touched (One file per line, +/- for only what was changed)

+ scripts/gddp.py — drop named checkout; detached merge + leased local ref publish
+ scripts/test_gddp.py — fixture when target_branch is the ordinary checkout
+ .handoffs/053-detached-acceptance-ref.md — this handoff

### Constrained areas touched (none / list + justification)

none — no graph YAML, verification, runtime, MyAPI, or MyAPI-rebuild edits

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Task files committed on `feat/graph-result-surface` and pushed to origin.

### Artifacts (Filepath - Description, 1 line max per artifact)

none

### Resume point (2-3 sentences max, anything more must be critically justifiable)

When the ordinary checkout is already on target_branch, `update-ref` moves the branch/HEAD to the merged tip and leaves the working tree files as they were. Remote push is still `refs/heads/<branch>:refs/heads/<branch>` without force.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
