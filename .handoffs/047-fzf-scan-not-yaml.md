# 047 — fzf filter is columns + a short card

------------------------------------------------ Agent Section START

Date: 2026-08-13
Worktree: /Users/sab-mini/repos/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Sab's `f` filter on gddp-dogfood was fzf with a 50% YAML pane and one unstyled `id  status · title` string per row. Labels now put a colored status column first; preview is an 8-line title/status/why card under the list (`down:8:wrap`).

### Scope touched (One file per line, +/- for only what was changed)

+ scripts/gddp.py — ANSI column labels; preview cmds call fzf_preview.py
+ scripts/fzf_pick.py — preview window down:8:wrap
+ scripts/fzf_preview.py — node/project cards
+ scripts/test_fzf_pick.py — label order + card + window
+ scripts/README.md — f preview contract
+ .handoffs/047-fzf-scan-not-yaml.md — this handoff

### Constrained areas touched (none / list + justification)

none

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Working tree has the six files above uncommitted on `main` tracking `origin/main` at `94b088f` before this commit.

### Artifacts (Filepath - Description, 1 line max per artifact)

none

### Resume point (2-3 sentences max, anything more must be critically justifiable)

`scripts.test_fzf_pick` + `scripts.test_gddp` after commit. Press `f` on nodes · gddp-dogfood to confirm columns + card.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
