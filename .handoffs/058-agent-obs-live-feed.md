# 058 — Agent-obs live feed

------------------------------------------------ Agent Section START

Date: 2026-09-10
Worktree: /home/sab-mini/gddp-config (khoj-38w)
Branch: main

## Empirical Reality

Live target watches and picker event actions now exec Layer 1's `agent-obs feed --watch`, joining attempt worktree to indexed session ID with retry-bound map fallback. All 353 script tests pass, including real native-session ingestion, streamed initial/appended events, and Ctrl-C; schema validation passes with 14 existing warnings.

### Scope touched

+/- scripts/gddp.py — session/CLI lookup, feed delegation, picker retry identity, help.
+ scripts/test_gddp_watch.py — lookup, errors, watch/jobs/picker routes, real CLI smoke test (skips only when Layer 1 CLI is unavailable).
+/- scripts/test_gddp.py — receipt fixture timestamps made deterministic after reproducing identical filesystem mtimes.
+ scripts/README.md — Layer 1 configuration and watch/snapshot behavior.
+ .handoffs/058-agent-obs-live-feed.md — this handoff.

### Constrained areas touched

None; canonical graphs, runtime job state, executor code, and Layer 1 source remain unchanged.

### Current Git state

Started clean, fast-forwarded main from c440d50 to 575ce3b. Test repair 602faca and integration 305c0d0 were committed and pushed; final handoff/test coverage checkpoint follows on main.

### Artifacts

scripts/test_gddp_watch.py — `.venv/bin/python -m pytest -q scripts/test_gddp_watch.py` reruns the integration checks with isolated DB/source/spool and forwarding disabled.

### Resume point

Wiring is shipped; this host currently has zero live attempts and an empty default Layer 1 index, so actual job feeds require the matching session to be ingested into `AGENT_OBS_DB`; the isolated real CLI test proves streaming independently of that host state. The verified companion executable is `/home/sab-mini/agent-observability/.venv/bin/agent-obs`; ByteRover query failed because the instructed `/Users/.../query.mjs` and Linux skill/record paths are absent, so continuity is recorded here.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
