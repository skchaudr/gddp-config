# 035 — TUI PTY hardening

------------------------------------------------ Agent Section START

Date: 2026-07-31
Worktree: /tmp/gddp-config-tui-hardening.Af29AD
Branch: fix/gddp-tui-pty-hardening

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

A real PTY reproduced delayed `ESC [ C` becoming Escape/back plus leaked `[C`; the decoder allowed only 30 ms and restored terminal state with input-flushing `TCSAFLUSH`. Commit `7f8c01d` raises the sequence window, preserves queued input, gives Up/Down cursor semantics, keeps Left/Right and p/n paging, and adds direct project browsing.

### Scope touched (One file per line, +/- for only what was changed)

+ scripts/terminal.py — robust split arrow decoding and non-flushing terminal restore
+ scripts/gddp.py — highlighted cursor navigation and direct `node browse` entry
+ scripts/test_terminal.py — real PTY delayed-arrow regression
+ scripts/test_gddp.py — cursor, paging, and direct-entry regressions
+ scripts/README.md — corrected keymap and browse command

### Constrained areas touched (none / list + justification)

none

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Implementation commit `7f8c01d` is isolated from concurrent unrelated changes in the primary checkout. The branch contains only this TUI patch and this handoff.

### Artifacts (Filepath - Description, 1 line max per artifact)

scripts/test_terminal.py - PTY regression for an 80 ms-split Right arrow.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

Merge or cherry-pick `fix/gddp-tui-pty-hardening` after reconciling the separate primary-checkout work. Validation: 28 CLI tests, 4 terminal tests, py_compile, diff check, two live PTY flows, and validator 0 errors with 2 pre-existing warnings.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)
