# 020 - Shareable graph bundles

------------------------------------------------ Agent Section START

Date: 2026-07-03
Worktree: /home/sab/gddp-config.worktrees/setup-multiple-agents-in-vm
Branch: agents/setup-multiple-agents-in-vm

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

Added a small exporter that folds each project graph's `project.yaml` plus its node YAML files into one shareable bundle. Generated bundle files for the five current project graphs under `graphify-out/shareable-graphs/`.

### Scope touched (One file per line, +/- for only what was changed)

+ scripts/export_graph_bundles.py
+ scripts/README.md
+ graphify-out/shareable-graphs/aa-cli.yaml
+ graphify-out/shareable-graphs/album-production.yaml
+ graphify-out/shareable-graphs/gddp-runtime.yaml
+ graphify-out/shareable-graphs/sell-valuables.yaml
+ graphify-out/shareable-graphs/vault-doctor.yaml

### Constrained areas touched (none / list + justification)

none

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Working tree was clean before this session. After generation, the tree shows the new exporter script, the docs note, and the derived bundle directory as untracked/modified work.

### Artifacts (Filepath - Description, 1 line max per artifact)

/home/sab/gddp-config.worktrees/setup-multiple-agents-in-vm/scripts/export_graph_bundles.py - exporter for shareable one-file project graph bundles.
/home/sab/gddp-config.worktrees/setup-multiple-agents-in-vm/graphify-out/shareable-graphs/aa-cli.yaml - example shareable bundle for aa-cli.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

If needed, rerun `python3 scripts/export_graph_bundles.py` after graph edits to refresh the shareable bundles, then validate the tree and commit. The only remaining decision is whether these derived bundles should stay tracked here or move to a different export location later.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point

## Narrative / Trajectory (SAB ONLY)

### Intent going into/at start of session

### Interpretation of how the session went

### Friction experienced or anticipated

### What's Next (Momentum or Lack Thereof)