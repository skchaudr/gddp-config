# 035 — Human-first evaluator output

------------------------------------------------ Agent Section START

Date: 2026-08-01
Worktree: /Users/sab-mini/repos/gddp-config
Branch: main

## Empirical Reality (2-3 sentences max, anything more must be critically justifiable)

The evaluation-only node view now leads with evaluator verdict, authored reasoning, recommendation, and findings. Runtime/job identifiers and node overview follow as provenance; focused CLI tests and a live read-only `gddp node show --view evaluation` passed.

### Scope touched (One file per line, +/- for only what was changed)

.gitignore + ignore generated `.pi-subagents/` run artifacts
scripts/node_cli.py + human-first evaluation rendering
scripts/test_node_cli.py + output-order/reasoning coverage
scripts/gddp.py + accurate `--full` help
scripts/README.md + documented hierarchy

### Constrained areas touched (none / list + justification)

Runtime evidence safety labels and graph-truth boundaries preserved; only read-only rendering changed.

### Current Git state (2-3 sentences max, anything more must be critically justifiable)

Main tracked origin/main at session start. Only the listed session changes and this handoff await the session commit/push.

### Artifacts (Filepath - Description, 1 line max per artifact)

None.

### Resume point (2-3 sentences max, anything more must be critically justifiable)

No implementation follow-up required. If refining UX, start from the live evaluation-only view and preserve current-vs-stale evidence labeling.

------------------------------------------------ Agent Section END

------------------------ Do NOT edit this file past this point
