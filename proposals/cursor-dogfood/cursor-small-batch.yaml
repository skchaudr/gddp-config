# Draft node; Sab supplies graph status at materialization.
schema_version: '1.0'
schema_type: node
node_id: cursor-small-batch
title: Run two or three nodes through Cursor
type: milestone
why: Exercise the actual startup and result-return path with hooks connected, using a small batch selected by Sab.
depends_on:
- cursor-hooks-attribution
acceptance_criteria:
- id: cursor-small-batch
  criterion: GDDP collects Cursor results for two or three selected nodes.
constraints:
- Run the selected bounded batch through the supported startup entrypoint; preserve unrelated queues and services.
- Treat retries as additional attempts of the same node.
allowed_execution_modes:
- cursor_cli
required_artifacts: []
human_gate: true
priority: high
unlocks:
- cursor-evaluator-reference-ack
