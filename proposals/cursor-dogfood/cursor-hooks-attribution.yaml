# Draft node; Sab supplies graph status at materialization.
schema_version: '1.0'
schema_type: node
node_id: cursor-hooks-attribution
title: Wire Cursor events into an existing trajectory view
type: capability
why: Start tracing Cursor activity as soon as its hooks are connected.
depends_on:
- cursor-project-session-restoration
acceptance_criteria:
- id: cursor-hooks-attribution
  criterion: Cursor activity appears in an existing trajectory view during execution.
constraints:
- Connect Cursor's installed hooks to an existing observation surface; keep this work scoped to event delivery.
allowed_execution_modes:
- cursor_cli
required_artifacts: []
human_gate: true
priority: high
unlocks:
- cursor-small-batch
