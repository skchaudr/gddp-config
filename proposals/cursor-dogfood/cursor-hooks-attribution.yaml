# Draft node; Sab supplies graph status at materialization.
schema_version: '1.0'
schema_type: node
node_id: cursor-hooks-attribution
title: Connect Cursor hooks and attribution
type: capability
why: Make Cursor activity observable at the correct project, session, node, attempt and worker, including installed
  skill and custom-subagent integration.
depends_on:
- cursor-project-session-restoration
acceptance_criteria:
- id: cursor-hooks-attribution
  criterion: A Cursor hook event resolves to the worker and node attempt that emitted it.
constraints:
- Use the installed Cursor capabilities and existing observation system; verify their actual interfaces before wiring
  them.
allowed_execution_modes:
- cursor_cli
required_artifacts: []
human_gate: true
priority: high
unlocks:
- cursor-small-batch
